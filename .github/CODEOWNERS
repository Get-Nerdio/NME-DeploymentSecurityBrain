# Require review on all knowledge changes before merge.
# (CODEOWNERS only enforces review if branch protection requiring it is enabled on `main`.)

*                 @drieling-nerdio @SeaneyC

# Permissions and hardening are security-sensitive — same reviewers, called out explicitly.
/knowledge/permissions/   @drieling-nerdio @SeaneyC
/knowledge/hardening/     @drieling-nerdio @SeaneyC
